{% set title = 'Render' %}
{{ generatePageBreadcrumbs(title, _self) | endTextBySeparatorRst }}

{{ "Documentation render" | textToHeading('H1') }}

This section describes how documents are rendered based on preset templates.

{{ drawDocumentationMenu(_self) }}