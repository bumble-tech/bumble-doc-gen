{% set title = 'Project files map' %}
{% set prevPage = 'Project structure' %}
{{ generatePageBreadcrumbs(title, _self) }}

{{ "Files map" | textToHeading('H1') }}

{{ drawClassMap( phpClassEntityCollection ) }}