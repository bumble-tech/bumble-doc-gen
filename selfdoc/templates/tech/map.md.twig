{% set title = 'Class map' %}
{% set prevPage = 'Technical description of the project' %}
{{ generatePageBreadcrumbs(title, _self) }}

Directory layout ( only documented files shown ):

{{ drawClassMap( phpEntities.filterByPaths(['/src']) ) }}