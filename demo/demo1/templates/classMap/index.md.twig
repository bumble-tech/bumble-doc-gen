---
title: Class map
---
{{ generatePageBreadcrumbs(title, _self) }}

# Class map example

```twig
{{ "{{ drawClassMap( phpEntities ) }}" }}
```

**The result of the function execution:**

{{ drawClassMap( phpEntities ) }}
